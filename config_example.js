// change the name of this file to config.js and run the build script

// change the value below to a random string of at least 16 characters
var salt = 'a12b3c4d5e_change_me';
// the username below will always be prefilled in the User field
var default_username = 'my_default_username';